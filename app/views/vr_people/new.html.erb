<div class="container">
  <div class="row">
    <div class="col-md-10 offset-md-1">
      <%= form_with model: @vr_person, url: vr_people_path, local: true do |f| %>

          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, class: "form-control mb-3" %>
          </div>

           <div class="form-group">
             <%= f.label :description %><br />
             <%= f.text_area :description %>
           </div>

          <div class="form-group">
            <%= f.label :message %>
            <%= f.text_field :message, class: "form-control mb-3" %>
          </div>

          <!--
          <div class="form-group">
            <%= f.label :画像 %>
            <%= f.file_field :profile_url, class: "form-control", accept: 'image/*', onchange: 'previewImage(preview)' %>
            <%= f.hidden_field :profile_url_cache %>
            <div class='mt-3 mb-3'>
              <% if @vr_person && @vr_person.profile_url %>
                <%= image_tag @vr_person.profile_url.url, id: 'preview', size: '100x100', class: 'rounded-circle' %>
              <% end %>
            </div>
          </div>

          -->
          <%= f.submit  class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>