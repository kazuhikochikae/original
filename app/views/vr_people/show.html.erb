<h3>キャラ名</h3>
<%= @vr_person.name %>

<h3>一言メッセージ</h3>
<%= @vr_person.message %>

<h3>自己紹介文</h3>
<%= @vr_person.description %>

<div class="avatar_box">
  <h3>プロフィール画像</h3>
  <% if @vr_person.avatar.attached? %>
    <%= image_tag url_for(@vr_person.avatar) %>
  <% else %>
    <p>No avatar uploaded.</p>
  <% end %>
</div>
<!--
<p class="chat_btn"><%= link_to 'チャットする', conversation_messages_path(Conversation.between(current_user.id, @vr_person.user.id).first.id) %></p>
-->