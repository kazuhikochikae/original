<!DOCTYPE html>
<html>
  <head>
    <title>Original</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

   
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100;200;300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/earlyaccess/nicomoji.css" rel="stylesheet">

  </head>

  <body>
  <header class="container sed">
  <h1 class="ttl">Character Talk<br><!--<span>キャラクター トーク</span>--></h1>
  <ul>
  <% if user_signed_in? %>
    <% if current_user.role == "user" %>
      <li><%= link_to "キャラ一覧", vr_people_path %></li>
    <% elsif current_user.role == "vr_person" %>
      <li><%= link_to "ユーザー一覧", profiles_path %></li>
    <% end %>
    <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
  <% else %>
      <li><%= link_to "新規登録", new_user_registration_path %></li>
      <li><%= link_to "ログイン", new_user_session_path %></li>
  <% end %>
  </ul>
  </header>
  <main>
 

  <% if notice %>
    <p class="alert alert-notice"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-error"><%= alert %></p>
  <% end %>

    <%= yield %>

  

</main>
<footer>© Character Talk, Inc. All rights reserverd.</footer>
  </body>
</html>
